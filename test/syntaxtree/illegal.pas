program BitcoinConversion;
var dollars, yen, bitcoins: integer;
begin
  dollars:= 1000000;
  yen:= dollars * 102;;
  bitcoins := potato/ 8500;

  write(dollars);
  write(yen);
  write(bitcoins);

end
.
